using UnityEngine;

public enum PlayerMoveState
{
    Idle,
    Move,
    Shade
}

public enum PlayerActionState
{
    None,
    Scanning
}

public enum PlayerControlState
{
    Normal,
    UI,
    GameOver
}